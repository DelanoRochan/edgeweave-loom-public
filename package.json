{
  "name": "proweave-apt-repository",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').accessSync('public/dists/stable/InRelease'); console.log('Signed APT repository ready')\""
  }
}
